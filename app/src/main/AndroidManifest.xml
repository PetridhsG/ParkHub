<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/round_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/round_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkHub"
        tools:targetApi="31">
        <activity
            android:name=".views.signIn.SignInActivity"
            android:exported="true"
            android:label="ParkHub"
            android:theme="@style/Theme.ParkHub.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.homepage.HomepageActivity" />
        <activity android:name=".views.signUpPersonal.SignUpPersonalActivity" />
        <activity android:name=".views.signUpVehicle.SignUpVehicleActivity" />
        <activity android:name=".views.spotDeclaration.SpotDeclarationActivity" />
        <activity android:name=".views.spotReservation.SpotReservationActivity" />
        <activity android:name=".views.rating.RatingActivity" />
        <activity android:name=".views.viewProfile.ViewProfileActivity" />
        <activity android:name=".views.editProfile.EditProfileActivity" />
        <activity android:name=".views.pBucksRenewal.PBucksRenewalActivity" />



    </application>

</manifest>
